{
	"private": true,
	"scripts": {
		"postinstall": "cd desk && yarn install && cd ../public_booking && yarn install",
		"dev": "cd desk && yarn dev",
		"dev:public": "cd public_booking && yarn dev",
		"build": "cd desk && yarn build && cd ../public_booking && yarn build",
		"build:desk": "cd desk && yarn build",
		"build:public": "cd public_booking && yarn build"
	},
	"workspaces": ["desk", "public_booking"]
}
